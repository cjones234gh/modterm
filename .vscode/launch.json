{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Launch (modterm)",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}",
            "preLaunchTask": "run-modterm-exe",
            "requireExactSource": false,
            "justMyCode": false
        },
        {
            "name": ".NET Core Attach",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}
