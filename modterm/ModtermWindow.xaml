<Window x:Class="modterm.ModtermWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:winuiex="using:WinUIEx" 
        xmlns:local="using:modterm"
        xmlns:modterm="using:modterm"
        xmlns:win2dcanvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
        mc:Ignorable="d"
        Title="modterm">

    <Grid x:Name="RootGrid" Background="Transparent">
        <!-- Draggable title bar -->
        <Grid x:Name="AppTitleBar"
              Height="33"
              VerticalAlignment="Top"
              Background="Transparent" />
        <!-- Single application pane -->
        <win2dcanvas:CanvasControl x:Name="ModtermCanvas"
                Margin="0,0,0,0"
                Background="Transparent"/>
    </Grid>
</Window>